{
  "name": "kudbee-quality-smoke",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright smoke suite for the Kudbee marketing site (DBZ-059, Polish Lane 12). Self-contained: its own package.json so the root repo stays zero-build. Not required for the site to run — CI-only.",
  "type": "module",
  "scripts": {
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  },
  "engines": { "node": ">=18.17.0" }
}
