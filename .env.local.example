# Local-only overrides template (PLACEHOLDERS ONLY — no real secrets)
#
# Copy to .env.local for values you only want on your machine:
#   cp .env.local.example .env.local
# .env.local and .env.*.local are gitignored. Never commit real secrets.

# Example local override (placeholder).
ANTHROPIC_API_KEY=
